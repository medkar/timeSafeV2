#pragma once
// Copy this file to  src/secrets.h  and fill in YOUR 2.4 GHz WiFi network.
// src/secrets.h is gitignored — your credentials are never published.
// (WiFi can also be configured on-screen at runtime; on-screen credentials
//  are stored in NVS and take priority over these values.)
#define WIFI_SSID "your-2.4GHz-network"
#define WIFI_PASS "your-password"
